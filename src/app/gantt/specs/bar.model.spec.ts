import { Bar } from './../bar.model';

describe('Bar model', () => {
  beforeEach(() => {
    let bar = new Bar();
    this.dimensions = bar.getDimensions();

  });

  it('should ', () => {
  });
});
